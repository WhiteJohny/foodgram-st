{{- if .Values.workers.nyt.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "foodgram.fullname" . }}-nyt-worker
  labels:
    {{- include "foodgram.labels" . | nindent 4 }}
    app.kubernetes.io/component: nyt-worker
spec:
  replicas: {{ .Values.workers.nyt.replicaCount }}
  selector:
    matchLabels:
      {{- include "foodgram.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: nyt-worker
  template:
    metadata:
      labels:
        {{- include "foodgram.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: nyt-worker
    spec:
      containers:
      - name: worker
        image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
        imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
        command: ["python"]
        args:
          - "workers/nyt_worker.py"
          - "--queue"
          - {{ .Values.workers.nyt.queue | quote }}
          - "--output-dir"
          - {{ .Values.workers.nyt.outputDir | quote }}
        env:
        - name: RABBITMQ_HOST
          value: {{ .Values.rabbitmq.host | default "my-rabbitmq.default.svc.cluster.local" | quote }}
        - name: RABBITMQ_PORT
          value: {{ .Values.rabbitmq.port | default "5672" | quote }}
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef:
              name: {{ .Values.rabbitmq.secretName }}
              key: {{ .Values.rabbitmq.userKey | default "username" }}
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.rabbitmq.secretName }}
              key: {{ .Values.rabbitmq.passwordKey | default "password" }}
        - name: NYT_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ .Values.nyt.secretName }}
              key: {{ .Values.nyt.apiKeyKey | default "NYT_API_KEY" }}
        volumeMounts:
        - name: output
          mountPath: {{ .Values.workers.nyt.outputDir }}
        resources:
          {{- toYaml .Values.workers.nyt.resources | nindent 10 }}
      volumes:
      - name: output
        {{- if .Values.workers.nyt.persistence.enabled }}
        persistentVolumeClaim:
          claimName: {{ include "foodgram.fullname" . }}-nyt-output
        {{- else }}
        emptyDir: {}
        {{- end }}
{{- end }}
