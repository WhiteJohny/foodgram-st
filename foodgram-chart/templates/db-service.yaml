apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: foodgramh
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/instance: my-foodgram
